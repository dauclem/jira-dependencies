type Issue @model {
  id: ID!
  name: String!
  description: String
  image: String
}

type Query {
  jiradepsJiraCall(url: String user: String password: String): String! @function(name: "jiradepsJiraCall-${env}")
}
